# scikit-learn is pinned for existing pickle model compatibility.
# Remove this exception after retraining models on a patched scikit-learn version.
PYSEC-2024-110

# python:3.11-slim-bookworm carries these packaging tools in its base layer;
# the production image removes them after the application virtualenv is copied.
# Keep the exception explicit until the upstream base image drops the metadata.
CVE-2026-23949
CVE-2026-24049
