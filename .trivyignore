# scikit-learn is pinned for existing pickle model compatibility.
# Remove this exception after retraining models on a patched scikit-learn version.
PYSEC-2024-110
# The runtime uses the patched /opt/venv. Trivy also reports stale global
# packaging metadata inherited from the Python builder layer; those packages
# are removed from the application path and are not imported at runtime.
GHSA-6v7p-g79w-8964
CVE-2025-47273
